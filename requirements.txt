# Backend deps for the Vercel Python serverless function (api/index.py).
# Mirrors backend/pyproject.toml runtime deps (no uvicorn needed serverless).
fastapi>=0.111
httpx>=0.27
numpy>=1.26
pandas>=2.2
scipy>=1.13
scikit-learn>=1.5
pydantic>=2.7
